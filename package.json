{
  "main": "dist/main.js",
  "type": "commonjs",
  "scripts": {
    "build": "npm update; npm install; tsc; npm prune --omit=dev"
  },
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0",
    "https": "^1.0.0",
    "tslib": "^2.5.0"
  },
  "devDependencies": {
    "ts-node": "^10.9.1"
  },
  "exclude": [
    "./node_modules/",
    "./dist/"
  ]
}
